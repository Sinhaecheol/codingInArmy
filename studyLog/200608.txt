1일차

p159에 있는 문제를 풀었다.
풀긴 했지만 진짜 아예 다 까먹었다.
심지어 Scanner를 어떻게 쓰는지 마저 까먹었다.
심각하다.
열심히 해야겠다.

현재 28p까지 읽었다.
알게된 점:
	-자바의 목표는 플랫폼 독립적, 메모리 사용량이 적음
	-플랫폼 독립적 => JVM으로 해결.
	-메모리 사용량 줄이기 =>링크의 과정을 없앴다.
	-.java파일은 소스코드, .class파일은 바이트코드
	-자바파일을 컴파일 하면 .class가 생성된다.
	 이 class파일을 디어셈블 하면 .bc 의 바이트코드 파일이 생성된다.
	 javac(컴파일), javap(디어셈블)같은 것들을 개발 소프트웨어라고 하고 이것들은 jdk안에 bin디렉터리에 있다.